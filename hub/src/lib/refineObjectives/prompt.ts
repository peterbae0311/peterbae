/**
 * AI 다듬기 기본 프롬프트 — 과정에 커스텀 프롬프트(courses.prompt)가 없을 때 쓰는 기본값.
 * 클라이언트("학습목표 프롬프트" 팝업의 기본 표시값)와 서버(refine 라우트의 폴백) 양쪽에서
 * 같은 값을 써야 해서 'server-only'를 붙이지 않은 공용 모듈로 둔다.
 */
export const DEFAULT_REFINE_PROMPT =
  '당신은 교육과정 학습목표 문장을 다듬는 전문가입니다. 주어진 학습목표 한 개를 더 명확하고 ' +
  '실행 가능한 문장으로 다듬어주세요. 원래 의미와 범위는 바꾸지 말고, 간결한 학습목표 어투 ' +
  '(예: "~할 수 있다")로 통일하세요. 다른 설명이나 따옴표 없이 다듬어진 문장 하나만 출력하세요.';
